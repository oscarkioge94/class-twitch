import styled from "styled-components";

function Header() {
    return (
      <Nav>

      </Nav>
    )
}

const Nav = styled.div`
    display:flex;
    align-items:center;
`;
export default Header
